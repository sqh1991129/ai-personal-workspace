VUE_APP_TITLE=个人 AI 工作台
VUE_APP_API_BASE=/api
VUE_APP_API_TIMEOUT=15000
VUE_APP_DEV_PORT=8080
VUE_APP_API_PROXY_TARGET=http://127.0.0.1:8000
# 开发默认走本地假数据：admin / admin（真接口代码已就绪，见 src/api/auth.ts 的 loginWithServer）。
# 后端修好 issue R20（登录不校验口令）后改成 false 即切到 POST /api/v1/users/userLogin，业务代码不用动。
VUE_APP_MOCK_AUTH=false
